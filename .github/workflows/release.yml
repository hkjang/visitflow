name: Release offline Docker image

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - uses: docker/setup-buildx-action@v4
      - name: Build offline image
        env:
          VERSION: ${{ github.ref_name }}
          COMMIT: ${{ github.sha }}
        run: |
          version="${VERSION#v}"
          built_at="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          docker buildx build --load --platform linux/amd64 \
            --build-arg "VERSION=${version}" \
            --build-arg "COMMIT=${COMMIT}" \
            --build-arg "BUILT_AT=${built_at}" \
            -t "visitflow:${version}" .
          docker save "visitflow:${version}" | gzip -9 > "VisitFlow-${VERSION}-linux-amd64-image.tar.gz"
      - name: Verify image archive
        env:
          VERSION: ${{ github.ref_name }}
        run: |
          test -s "VisitFlow-${VERSION}-linux-amd64-image.tar.gz"
          gzip -t "VisitFlow-${VERSION}-linux-amd64-image.tar.gz"
      - name: Create GitHub release with image only
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ github.ref_name }}
        run: |
          gh release create "$VERSION" \
            "VisitFlow-${VERSION}-linux-amd64-image.tar.gz" \
            --verify-tag \
            --generate-notes \
            --title "VisitFlow ${VERSION}"
